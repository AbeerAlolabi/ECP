…or create a new repository on the command line


echo "# ECP" >> README.md
git init
git add README.md
git commit -m "first commit"
git branch -M main
git remote add origin https://github.com/AbeerAlolabi/ECP.git
git push -u origin main




to pull the reposetory into new file use the following command:
git clone https://github.com/AbeerAlolabi/ECP.git 